#include <iostream>
#include <vector>
#include <algorithm>
#include <functional>
using namespace std;
int main()
{
    // Q: assign iterator i to vector that hold float in reverse order
    // A: code below
    vector<float>::reverse_iterator i;

    // breakdown
    // 1. assign iterator i
    reverse_iterator i;

    // 2. vector hold float
    vector<float>;

    return 0;
}