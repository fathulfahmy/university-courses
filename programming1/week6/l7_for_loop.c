#include <stdio.h>

int main(void)
{
    int i;

    // for(initial value is 1, loop less than 11 times, increment i after every loop)
    for (i = 1; i < 11; ++i)
    {
        printf("%d ", i);
    }
    return 0;
}
